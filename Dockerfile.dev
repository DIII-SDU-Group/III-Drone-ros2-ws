ARG ROS_DISTRO

FROM osrf/ros:humble-desktop-full
# FROM nvidia/cuda:12.4.0-base-ubuntu22.04

# Create the user
RUN groupadd --gid 1000 iii
RUN useradd --uid 1000 --gid 1000 -m iii
RUN apt-get update
RUN apt-get install -y sudo
RUN echo iii ALL=\(root\) NOPASSWD:ALL > /etc/sudoers.d/iii
RUN chmod 0440 /etc/sudoers.d/iii
RUN apt-get update && apt-get upgrade -y
RUN usermod -s /bin/bash iii

# Install dependencies
COPY src/III-Drone-Core/scripts /scripts

# Install ROS
RUN \
    apt update && apt install -y locales wget && \
    wget https://packages.osrfoundation.org/gazebo.gpg -O /usr/share/keyrings/pkgs-osrf-archive-keyring.gpg && \
    echo "deb [arch=$(dpkg --print-architecture) signed-by=/usr/share/keyrings/pkgs-osrf-archive-keyring.gpg] http://packages.osrfoundation.org/gazebo/ubuntu-stable $(lsb_release -cs) main" | tee /etc/apt/sources.list.d/gazebo-stable.list > /dev/null && \
    apt update && \
    apt install -y ros-humble-rmw-cyclonedds-cpp && \
    /scripts/install_dependencies.sh && \
    rm -rf /scripts && \
    apt update && \
    apt remove -y ros-humble-ros-gz* && \
    apt install -y ros-humble-ros-gzgarden

# Install necessary packages and dependencies
RUN apt-get update && apt-get install -y \
    python3-colcon-common-extensions \
    python3-pip

RUN apt-get update && apt-get upgrade -y

# Copy the entrypoint script
COPY entrypoint_dev.sh /entrypoint.sh
RUN chmod +x /entrypoint.sh

# Create workspace folder and set ownership
RUN mkdir -p /home/iii/ws
RUN chown -R iii:iii /home/iii/ws

WORKDIR /home/iii/ws

# Set the entrypoint
ENTRYPOINT ["/entrypoint.sh"]

RUN echo "source /opt/ros/humble/setup.bash" >> /etc/bash.bashrc
RUN echo "source /home/iii/ws/install/setup.bash" >> /etc/bash.bashrc

# Install userspace tools
USER iii

RUN sudo apt-get install vim -y

COPY requirements.txt /requirements.txt

RUN sudo chown -R iii:iii /requirements.txt

RUN pip3 install --upgrade pip
RUN pip3 install -r /requirements.txt

RUN sudo rm -rf /requirements.txt

# Install simulation tools:
RUN sudo apt install -y wget git lsb-release

# QGroundControl
RUN sudo usermod -a -G dialout iii
RUN sudo apt-get remove modemmanager -y
RUN sudo apt install gstreamer1.0-plugins-bad gstreamer1.0-libav gstreamer1.0-gl -y
RUN sudo apt install libfuse2 -y
RUN sudo apt install libxcb-xinerama0 libxkbcommon-x11-0 libxcb-cursor0 -y
RUN sudo apt install -y fuse

RUN wget https://d176tv9ibo4jno.cloudfront.net/builds/master/QGroundControl.AppImage -P /home/iii
RUN chmod +x /home/iii/QGroundControl.AppImage

# Install development tools
RUN sudo apt install -y terminator tmux tmuxinator htop gdb
COPY tools/terminator /home/iii/.config/terminator
COPY tools/QGroundControl.org /home/iii/.config/QGroundControl.org
RUN sudo ln -s /home/iii/ws/tools/tmuxinator /home/iii/.config/tmuxinator

RUN sudo chown -R iii:iii /home/iii/.config

# Update bashrc
COPY .bashrc /home/iii/.bashrc
RUN sudo chown iii:iii /home/iii/.bashrc

ENV SHELL /bin/bash

# USER iii
