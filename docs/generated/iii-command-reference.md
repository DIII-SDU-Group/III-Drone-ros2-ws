# Generated III Command Reference

Generated by `deployment/scripts/update_documentation_references.py`.
Do not edit by hand. Universal structured output uses `--output=json`.

## `iii access enroll add`

- Mutating: `yes`
- Interactive terminal: `no`

```text
usage: iii access enroll add [-h] --enrollment ENROLLMENT
                             [--target {real,opti_track,hil}]

options:
  -h, --help            show this help message and exit
  --enrollment ENROLLMENT
  --target {real,opti_track,hil}
```

## `iii access enroll prepare`

- Mutating: `yes`
- Interactive terminal: `no`

```text
usage: iii access enroll prepare [-h] --directory DIRECTORY --label LABEL
                                 (--signer-passphrase-file SIGNER_PASSPHRASE_FILE | --keyring-account KEYRING_ACCOUNT)

options:
  -h, --help            show this help message and exit
  --directory DIRECTORY
  --label LABEL
  --signer-passphrase-file SIGNER_PASSPHRASE_FILE
  --keyring-account KEYRING_ACCOUNT
```

## `iii access enroll prove`

- Mutating: `yes`
- Interactive terminal: `no`

```text
usage: iii access enroll prove [-h] --enrollment ENROLLMENT
                               [--target {real,opti_track,hil}]

options:
  -h, --help            show this help message and exit
  --enrollment ENROLLMENT
  --target {real,opti_track,hil}
```

## `iii access list`

- Mutating: `no`
- Interactive terminal: `no`

```text
usage: iii access list [-h] [--target {real,opti_track,hil}]

options:
  -h, --help            show this help message and exit
  --target {real,opti_track,hil}
```

## `iii access revoke`

- Mutating: `yes`
- Interactive terminal: `no`

```text
usage: iii access revoke [-h] --machine-id MACHINE_ID
                         [--target {real,opti_track,hil}]

options:
  -h, --help            show this help message and exit
  --machine-id MACHINE_ID
  --target {real,opti_track,hil}
```

## `iii access signer revoke`

- Mutating: `yes`
- Interactive terminal: `no`

```text
usage: iii access signer revoke [-h] --signer-id FIELD_SIGNER_ID
                                [--target {real,opti_track,hil}]

options:
  -h, --help            show this help message and exit
  --signer-id FIELD_SIGNER_ID
  --target {real,opti_track,hil}
```

## `iii config capture delete`

- Mutating: `yes`
- Interactive terminal: `no`

```text
usage: iii config capture delete [-h] --target {real,sim} --snapshot SNAPSHOT
                                 [--capture-id CAPTURE_ID] [--force]
                                 [--confirm-snapshot CONFIRM_SNAPSHOT]
                                 [--capture-root CAPTURE_ROOT]

options:
  -h, --help            show this help message and exit
  --target {real,sim}
  --snapshot SNAPSHOT
  --capture-id CAPTURE_ID
  --force
  --confirm-snapshot CONFIRM_SNAPSHOT
  --capture-root CAPTURE_ROOT
```

## `iii config capture diff`

- Mutating: `no`
- Interactive terminal: `no`

```text
usage: iii config capture diff [-h] [--against AGAINST]
                               [--capture-root CAPTURE_ROOT]
                               capture_id

positional arguments:
  capture_id

options:
  -h, --help            show this help message and exit
  --against AGAINST
  --capture-root CAPTURE_ROOT
```

## `iii config capture export`

- Mutating: `yes`
- Interactive terminal: `no`

```text
usage: iii config capture export [-h] [--capture-id CAPTURE_ID] --archive
                                 ARCHIVE [--capture-root CAPTURE_ROOT]

options:
  -h, --help            show this help message and exit
  --capture-id CAPTURE_ID
  --archive ARCHIVE
  --capture-root CAPTURE_ROOT
```

## `iii config capture import`

- Mutating: `yes`
- Interactive terminal: `no`

```text
usage: iii config capture import [-h] [--capture-root CAPTURE_ROOT] archive

positional arguments:
  archive

options:
  -h, --help            show this help message and exit
  --capture-root CAPTURE_ROOT
```

## `iii config capture list`

- Mutating: `no`
- Interactive terminal: `no`

```text
usage: iii config capture list [-h] [--capture-root CAPTURE_ROOT]

options:
  -h, --help            show this help message and exit
  --capture-root CAPTURE_ROOT
```

## `iii config capture pull`

- Mutating: `yes`
- Interactive terminal: `no`

```text
usage: iii config capture pull [-h] --target {real,sim} --snapshot SNAPSHOT
                               --name NAME --description DESCRIPTION
                               [--capture-root CAPTURE_ROOT]

options:
  -h, --help            show this help message and exit
  --target {real,sim}
  --snapshot SNAPSHOT   saved target snapshot ID/path (repeat with --name and
                        --description)
  --name NAME           short display name paired with each --snapshot
  --description DESCRIPTION
                        purpose paired with each --snapshot
  --capture-root CAPTURE_ROOT
```

## `iii config capture show`

- Mutating: `no`
- Interactive terminal: `no`

```text
usage: iii config capture show [-h] [--capture-root CAPTURE_ROOT] capture_id

positional arguments:
  capture_id

options:
  -h, --help            show this help message and exit
  --capture-root CAPTURE_ROOT
```

## `iii config capture verify`

- Mutating: `no`
- Interactive terminal: `no`

```text
usage: iii config capture verify [-h] [--capture-root CAPTURE_ROOT]
                                 [capture_id ...]

positional arguments:
  capture_id

options:
  -h, --help            show this help message and exit
  --capture-root CAPTURE_ROOT
```

## `iii config promotion apply`

- Mutating: `yes`
- Interactive terminal: `no`

```text
usage: iii config promotion apply [-h] --capture-id CAPTURE_ID --profile
                                  {real,sim} --release-id RELEASE_ID --key KEY
                                  --classification {shared-tracked-default}
                                  [--base {develop}]
                                  [--capture-root CAPTURE_ROOT] [--commit]

options:
  -h, --help            show this help message and exit
  --capture-id CAPTURE_ID
  --profile {real,sim}
  --release-id RELEASE_ID
  --key KEY
  --classification {shared-tracked-default}
  --base {develop}
  --capture-root CAPTURE_ROOT
  --commit              create coordinated configuration and workspace commits
```

## `iii config promotion plan`

- Mutating: `no`
- Interactive terminal: `no`

```text
usage: iii config promotion plan [-h] --capture-id CAPTURE_ID --profile
                                 {real,sim} --release-id RELEASE_ID --key KEY
                                 --classification {shared-tracked-default}
                                 [--base {develop}]
                                 [--capture-root CAPTURE_ROOT]

options:
  -h, --help            show this help message and exit
  --capture-id CAPTURE_ID
  --profile {real,sim}
  --release-id RELEASE_ID
  --key KEY
  --classification {shared-tracked-default}
  --base {develop}
  --capture-root CAPTURE_ROOT
```

## `iii config sim checkpoint`

- Mutating: `yes`
- Interactive terminal: `no`

```text
usage: iii config sim checkpoint [-h]

options:
  -h, --help  show this help message and exit
```

## `iii config sim inspect`

- Mutating: `no`
- Interactive terminal: `no`

```text
usage: iii config sim inspect [-h]

options:
  -h, --help  show this help message and exit
```

## `iii config sim reset`

- Mutating: `yes`
- Interactive terminal: `no`

```text
usage: iii config sim reset [-h] [--restore CHECKPOINT_ID]

options:
  -h, --help            show this help message and exit
  --restore CHECKPOINT_ID
                        restore this verified checkpoint
```

## `iii config sim review`

- Mutating: `yes`
- Interactive terminal: `no`

```text
usage: iii config sim review [-h] [--decision SET:PARAMETER=CHOICE]

options:
  -h, --help            show this help message and exit
  --decision SET:PARAMETER=CHOICE
                        choose use_old or use_new_default for one exact review
                        item
```

## `iii deploy activate`

- Mutating: `yes`
- Interactive terminal: `no`

```text
usage: iii deploy activate [-h] --configuration-checkpoint-id
                           CONFIGURATION_CHECKPOINT_ID [--qualified]
                           [--decision SET:PARAMETER=CHOICE]
                           [--target {sim,real,hil}]
                           release_id

positional arguments:
  release_id

options:
  -h, --help            show this help message and exit
  --configuration-checkpoint-id CONFIGURATION_CHECKPOINT_ID
                        current/source checkpoint for activation; paired
                        rollback checkpoint for explicit rollback
  --qualified           declare explicit qualified activation authority
  --decision SET:PARAMETER=CHOICE
                        resolve one reviewed reintroduction with use_old or
                        use_new_default
  --target {sim,real,hil}
                        explicit per-command runtime target
```

## `iii deploy configuration-capture`

- Mutating: `no`
- Interactive terminal: `no`

```text
usage: iii deploy configuration-capture [-h] [--target {sim,real,hil}]

options:
  -h, --help            show this help message and exit
  --target {sim,real,hil}
                        explicit per-command runtime target
```

## `iii deploy continue`

- Mutating: `yes`
- Interactive terminal: `no`

```text
usage: iii deploy continue [-h] --decision SET:PARAMETER=CHOICE
                           [--target {sim,real,hil}]
                           review_operation_id

positional arguments:
  review_operation_id

options:
  -h, --help            show this help message and exit
  --decision SET:PARAMETER=CHOICE
                        resolve one exact retained review item with use_old or
                        use_new_default
  --target {sim,real,hil}
                        explicit per-command runtime target
```

## `iii deploy diagnostics pull`

- Mutating: `yes`
- Interactive terminal: `no`

```text
usage: iii deploy diagnostics pull [-h] [--destination DESTINATION]
                                   [--target {sim,real,hil}]

options:
  -h, --help            show this help message and exit
  --destination DESTINATION
  --target {sim,real,hil}
                        explicit per-command runtime target
```

## `iii deploy field`

- Mutating: `yes`
- Interactive terminal: `no`

```text
usage: iii deploy field [-h] --bundle-set BUNDLE_SET
                        [--configuration-checkpoint-id CONFIGURATION_CHECKPOINT_ID]
                        [--status-index STATUS_INDEX]
                        [--trusted-signers TRUSTED_SIGNERS]
                        [--component {gc,drone,both}]
                        [--include-mission INCLUDE_MISSION]
                        [--exclude-mission EXCLUDE_MISSION] [--activate]
                        [--decision SET:PARAMETER=CHOICE]
                        [--gc-override-reason GC_OVERRIDE_REASON]
                        [--gc-override-confirmation GC_OVERRIDE_CONFIRMATION]
                        [--target {sim,real,hil}]

options:
  -h, --help            show this help message and exit
  --bundle-set BUNDLE_SET
  --configuration-checkpoint-id CONFIGURATION_CHECKPOINT_ID
                        currently selected source checkpoint reconciled by
                        receiver activation (required with --activate; staging
                        does not require one)
  --status-index STATUS_INDEX
  --trusted-signers TRUSTED_SIGNERS
                        field bundle signer trust store
  --component {gc,drone,both}
  --include-mission INCLUDE_MISSION
  --exclude-mission EXCLUDE_MISSION
  --activate
  --decision SET:PARAMETER=CHOICE
                        resolve reviewed reintroduction values during drone
                        activation
  --gc-override-reason GC_OVERRIDE_REASON
                        separately audited GC recovery reason when receiver
                        safety is unavailable
  --gc-override-confirmation GC_OVERRIDE_CONFIRMATION
                        exact GC recovery warning confirmation
  --target {sim,real,hil}
                        explicit per-command runtime target
```

## `iii deploy inspect`

- Mutating: `no`
- Interactive terminal: `no`

```text
usage: iii deploy inspect [-h] [--trusted-signers TRUSTED_SIGNERS]
                          [--target {sim,real,hil}]
                          component

positional arguments:
  component

options:
  -h, --help            show this help message and exit
  --trusted-signers TRUSTED_SIGNERS
                        bundle signer trust store (defaults to
                        III_RELEASE_TRUSTED_SIGNERS)
  --target {sim,real,hil}
                        explicit per-command runtime target
```

## `iii deploy operations list`

- Mutating: `no`
- Interactive terminal: `no`

```text
usage: iii deploy operations list [-h]

options:
  -h, --help  show this help message and exit
```

## `iii deploy operations prune`

- Mutating: `yes`
- Interactive terminal: `no`

```text
usage: iii deploy operations prune [-h] --days DAYS --status
                                   {completed,cancelled}

options:
  -h, --help            show this help message and exit
  --days DAYS
  --status {completed,cancelled}
```

## `iii deploy operations show`

- Mutating: `no`
- Interactive terminal: `no`

```text
usage: iii deploy operations show [-h] operation

positional arguments:
  operation

options:
  -h, --help  show this help message and exit
```

## `iii deploy plan`

- Mutating: `no`
- Interactive terminal: `no`

```text
usage: iii deploy plan [-h] [--bundle-set BUNDLE_SET]
                       [--component {gc,drone,both}]
                       [--include-mission INCLUDE_MISSION]
                       [--exclude-mission EXCLUDE_MISSION]
                       [--target {sim,real,hil}]

options:
  -h, --help            show this help message and exit
  --bundle-set BUNDLE_SET
  --component {gc,drone,both}
  --include-mission INCLUDE_MISSION
  --exclude-mission EXCLUDE_MISSION
  --target {sim,real,hil}
                        explicit per-command runtime target
```

## `iii deploy receiver-update apply`

- Mutating: `yes`
- Interactive terminal: `no`

```text
usage: iii deploy receiver-update apply [-h] --trust TRUST
                                        [--target {sim,real,hil}]
                                        bundle

positional arguments:
  bundle

options:
  -h, --help            show this help message and exit
  --trust TRUST
  --target {sim,real,hil}
                        explicit per-command runtime target
```

## `iii deploy receiver-update inspect`

- Mutating: `no`
- Interactive terminal: `no`

```text
usage: iii deploy receiver-update inspect [-h] --trust TRUST bundle

positional arguments:
  bundle

options:
  -h, --help     show this help message and exit
  --trust TRUST
```

## `iii deploy rollback`

- Mutating: `yes`
- Interactive terminal: `no`

```text
usage: iii deploy rollback [-h] --configuration-checkpoint-id
                           CONFIGURATION_CHECKPOINT_ID
                           [--target {sim,real,hil}]
                           release_id

positional arguments:
  release_id

options:
  -h, --help            show this help message and exit
  --configuration-checkpoint-id CONFIGURATION_CHECKPOINT_ID
                        current/source checkpoint for activation; paired
                        rollback checkpoint for explicit rollback
  --target {sim,real,hil}
                        explicit per-command runtime target
```

## `iii deploy stage`

- Mutating: `yes`
- Interactive terminal: `no`

```text
usage: iii deploy stage [-h] [--status-index STATUS_INDEX]
                        [--target {sim,real,hil}]
                        component

positional arguments:
  component

options:
  -h, --help            show this help message and exit
  --status-index STATUS_INDEX
  --target {sim,real,hil}
                        explicit per-command runtime target
```

## `iii deploy status`

- Mutating: `no`
- Interactive terminal: `no`

```text
usage: iii deploy status [-h] [--target {sim,real,hil}]
                         [--operation OPERATION]

options:
  -h, --help            show this help message and exit
  --target {sim,real,hil}
                        explicit per-command runtime target
  --operation OPERATION
                        include the exact retained local operation records
```

## `iii docs check`

- Mutating: `no`
- Interactive terminal: `no`

```text
usage: iii docs check [-h] [--root ROOT]

options:
  -h, --help   show this help message and exit
  --root ROOT
```

## `iii field acknowledge`

- Mutating: `yes`
- Interactive terminal: `no`

```text
usage: iii field acknowledge [-h] --finding FINDING --rationale RATIONALE
                             --signing-key SIGNING_KEY
                             [--trusted-signers TRUSTED_SIGNERS]
                             record

positional arguments:
  record

options:
  -h, --help            show this help message and exit
  --finding FINDING
  --rationale RATIONALE
  --signing-key SIGNING_KEY
  --trusted-signers TRUSTED_SIGNERS
                        trusted signer store containing the active
                        workstation-field key
```

## `iii field check`

- Mutating: `no`
- Interactive terminal: `no`

```text
usage: iii field check [-h] [--target {sim,real,hil}] [--state STATE]
                       [--signing-key SIGNING_KEY]
                       [--trusted-signers TRUSTED_SIGNERS]

options:
  -h, --help            show this help message and exit
  --target {sim,real,hil}
  --state STATE         explicit observation fixture (testing/offline
                        diagnostics)
  --signing-key SIGNING_KEY
                        authorized field Ed25519 private key
  --trusted-signers TRUSTED_SIGNERS
                        trusted signer store containing the active
                        workstation-field key
```

## `iii field prepare`

- Mutating: `yes`
- Interactive terminal: `no`

```text
usage: iii field prepare [-h] [--offline] [--target {sim,real}]
                         [--repository REPOSITORY] [--schema-root SCHEMA_ROOT]
                         [--policy POLICY] [--trusted-signers TRUSTED_SIGNERS]
                         [--status-trusted-signers STATUS_TRUSTED_SIGNERS]
                         [--cache-root CACHE_ROOT]
                         version [version ...]

positional arguments:
  version               qualified SemVer tag(s) to prepare

options:
  -h, --help            show this help message and exit
  --offline             verify only existing cached status and artifacts
  --target {sim,real}
  --repository REPOSITORY
                        GitHub owner/repository
  --schema-root SCHEMA_ROOT
                        deployment schema directory
  --policy POLICY       operational policy JSON
  --trusted-signers TRUSTED_SIGNERS
                        qualified bundle signer trust store
  --status-trusted-signers STATUS_TRUSTED_SIGNERS
                        release-status signer trust store
  --cache-root CACHE_ROOT
                        qualified release cache root
```

## `iii field verify`

- Mutating: `no`
- Interactive terminal: `no`

```text
usage: iii field verify [-h] [--offline] [--target {sim,real}]
                        [--repository REPOSITORY] [--schema-root SCHEMA_ROOT]
                        [--policy POLICY] [--trusted-signers TRUSTED_SIGNERS]
                        [--status-trusted-signers STATUS_TRUSTED_SIGNERS]
                        [--cache-root CACHE_ROOT]
                        version [version ...]

positional arguments:
  version               qualified SemVer tag(s) already prepared locally

options:
  -h, --help            show this help message and exit
  --offline             require network-free, target-free cache verification
  --target {sim,real}
  --repository REPOSITORY
                        GitHub owner/repository
  --schema-root SCHEMA_ROOT
                        deployment schema directory
  --policy POLICY       operational policy JSON
  --trusted-signers TRUSTED_SIGNERS
                        qualified bundle signer trust store
  --status-trusted-signers STATUS_TRUSTED_SIGNERS
                        release-status signer trust store
  --cache-root CACHE_ROOT
                        qualified release cache root
```

## `iii gc application activate`

- Mutating: `yes`
- Interactive terminal: `no`

```text
usage: iii gc application activate [-h] --release-id RELEASE_ID
                                   [--trusted-signers TRUSTED_SIGNERS]
                                   (--safety-file SAFETY_FILE | --disconnected | --sim)
                                   [--override-reason OVERRIDE_REASON]
                                   [--override-confirmation OVERRIDE_CONFIRMATION]

options:
  -h, --help            show this help message and exit
  --release-id RELEASE_ID
  --trusted-signers TRUSTED_SIGNERS
                        release signer trust store; overrides
                        III_GC_TRUSTED_SIGNERS for this authenticated
                        operation
  --safety-file SAFETY_FILE
                        fresh authenticated maintenance-safety JSON
  --disconnected        explicitly assert that no aircraft target is connected
  --sim                 explicitly select the simulation safety profile
  --override-reason OVERRIDE_REASON
                        separately audited recovery reason
  --override-confirmation OVERRIDE_CONFIRMATION
                        exact recovery warning confirmation
```

## `iii gc application prune`

- Mutating: `yes`
- Interactive terminal: `no`

```text
usage: iii gc application prune [-h]

options:
  -h, --help  show this help message and exit
```

## `iii gc application reconcile`

- Mutating: `yes`
- Interactive terminal: `no`

```text
usage: iii gc application reconcile [-h]

options:
  -h, --help  show this help message and exit
```

## `iii gc application rollback`

- Mutating: `yes`
- Interactive terminal: `no`

```text
usage: iii gc application rollback [-h] [--trusted-signers TRUSTED_SIGNERS]
                                   (--safety-file SAFETY_FILE | --disconnected | --sim)
                                   [--override-reason OVERRIDE_REASON]
                                   [--override-confirmation OVERRIDE_CONFIRMATION]

options:
  -h, --help            show this help message and exit
  --trusted-signers TRUSTED_SIGNERS
                        release signer trust store; overrides
                        III_GC_TRUSTED_SIGNERS for this authenticated
                        operation
  --safety-file SAFETY_FILE
                        fresh authenticated maintenance-safety JSON
  --disconnected        explicitly assert that no aircraft target is connected
  --sim                 explicitly select the simulation safety profile
  --override-reason OVERRIDE_REASON
                        separately audited recovery reason
  --override-confirmation OVERRIDE_CONFIRMATION
                        exact recovery warning confirmation
```

## `iii gc application stage`

- Mutating: `yes`
- Interactive terminal: `no`

```text
usage: iii gc application stage [-h] --bundle BUNDLE [--protect-offline]
                                [--trusted-signers TRUSTED_SIGNERS]

options:
  -h, --help            show this help message and exit
  --bundle BUNDLE
  --protect-offline     retain this cached bundle as an operator-designated
                        offline set
  --trusted-signers TRUSTED_SIGNERS
                        release signer trust store; overrides
                        III_GC_TRUSTED_SIGNERS for this authenticated
                        operation
```

## `iii gc application status`

- Mutating: `no`
- Interactive terminal: `no`

```text
usage: iii gc application status [-h]

options:
  -h, --help  show this help message and exit
```

## `iii gc open`

- Mutating: `yes`
- Interactive terminal: `no`

```text
usage: iii gc open [-h]

options:
  -h, --help  show this help message and exit
```

## `iii gc provision`

- Mutating: `yes`
- Interactive terminal: `no`

```text
usage: iii gc provision [-h] [--offline] [--offline-cache OFFLINE_CACHE]
                        [--replacement-archive REPLACEMENT_ARCHIVE]
                        [--schema-root SCHEMA_ROOT]
                        [--ansible-playbook ANSIBLE_PLAYBOOK]

options:
  -h, --help            show this help message and exit
  --offline             use only a verified prepared-offline cache
  --offline-cache OFFLINE_CACHE
                        prepared-offline cache root
  --replacement-archive REPLACEMENT_ARCHIVE
                        verified P2.T8 portable archive for a fresh
                        replacement GC
  --schema-root SCHEMA_ROOT
                        override deployment schema root
  --ansible-playbook ANSIBLE_PLAYBOOK
                        explicit repository-managed ansible-playbook
                        executable
```

## `iii gc restart`

- Mutating: `yes`
- Interactive terminal: `no`

```text
usage: iii gc restart [-h]

options:
  -h, --help  show this help message and exit
```

## `iii gc start`

- Mutating: `yes`
- Interactive terminal: `no`

```text
usage: iii gc start [-h]

options:
  -h, --help  show this help message and exit
```

## `iii gc status`

- Mutating: `no`
- Interactive terminal: `no`

```text
usage: iii gc status [-h] [--schema-root SCHEMA_ROOT]

options:
  -h, --help            show this help message and exit
  --schema-root SCHEMA_ROOT
```

## `iii gc stop`

- Mutating: `yes`
- Interactive terminal: `no`

```text
usage: iii gc stop [-h]

options:
  -h, --help  show this help message and exit
```

## `iii host backup create`

- Mutating: `yes`
- Interactive terminal: `no`

```text
usage: iii host backup create [-h] [--registry-root REGISTRY_ROOT]
                              [--target TARGET]

options:
  -h, --help            show this help message and exit
  --registry-root REGISTRY_ROOT
  --target TARGET
```

## `iii host backup export`

- Mutating: `yes`
- Interactive terminal: `no`

```text
usage: iii host backup export [-h] [--registry-root REGISTRY_ROOT]
                              --destination DESTINATION
                              backup_id

positional arguments:
  backup_id

options:
  -h, --help            show this help message and exit
  --registry-root REGISTRY_ROOT
  --destination DESTINATION
```

## `iii host backup import`

- Mutating: `yes`
- Interactive terminal: `no`

```text
usage: iii host backup import [-h] [--registry-root REGISTRY_ROOT] --archive
                              ARCHIVE

options:
  -h, --help            show this help message and exit
  --registry-root REGISTRY_ROOT
  --archive ARCHIVE
```

## `iii host backup list`

- Mutating: `no`
- Interactive terminal: `no`

```text
usage: iii host backup list [-h] [--registry-root REGISTRY_ROOT]

options:
  -h, --help            show this help message and exit
  --registry-root REGISTRY_ROOT
```

## `iii host backup prune`

- Mutating: `yes`
- Interactive terminal: `no`

```text
usage: iii host backup prune [-h] [--registry-root REGISTRY_ROOT] backup_id

positional arguments:
  backup_id

options:
  -h, --help            show this help message and exit
  --registry-root REGISTRY_ROOT
```

## `iii host backup restore`

- Mutating: `yes`
- Interactive terminal: `no`

```text
usage: iii host backup restore [-h] [--registry-root REGISTRY_ROOT]
                               [--target TARGET]
                               backup_id

positional arguments:
  backup_id

options:
  -h, --help            show this help message and exit
  --registry-root REGISTRY_ROOT
  --target TARGET
```

## `iii host backup show`

- Mutating: `no`
- Interactive terminal: `no`

```text
usage: iii host backup show [-h] [--registry-root REGISTRY_ROOT] backup_id

positional arguments:
  backup_id

options:
  -h, --help            show this help message and exit
  --registry-root REGISTRY_ROOT
```

## `iii host backup status`

- Mutating: `no`
- Interactive terminal: `no`

```text
usage: iii host backup status [-h] [--registry-root REGISTRY_ROOT]
                              [--target TARGET]

options:
  -h, --help            show this help message and exit
  --registry-root REGISTRY_ROOT
  --target TARGET
```

## `iii host backup verify`

- Mutating: `no`
- Interactive terminal: `no`

```text
usage: iii host backup verify [-h] [--registry-root REGISTRY_ROOT] [backup_id]

positional arguments:
  backup_id

options:
  -h, --help            show this help message and exit
  --registry-root REGISTRY_ROOT
```

## `iii host hardware inspect`

- Mutating: `no`
- Interactive terminal: `no`

```text
usage: iii host hardware inspect [-h] [--target {real,opti_track,hil}]
                                 [--capture CAPTURE]

options:
  -h, --help            show this help message and exit
  --target {real,opti_track,hil}
  --capture CAPTURE     create one owner-only canonical capture; existing
                        files are never overwritten
```

## `iii host image inspect`

- Mutating: `no`
- Interactive terminal: `no`

```text
usage: iii host image inspect [-h] --image IMAGE --bootstrap-input
                              BOOTSTRAP_INPUT [--schema-root SCHEMA_ROOT]

options:
  -h, --help            show this help message and exit
  --image IMAGE         downloaded pinned .img.xz file
  --bootstrap-input BOOTSTRAP_INPUT
                        Git-ignored owner-only per-imaging JSON input
  --schema-root SCHEMA_ROOT
                        override deployment schema directory
```

## `iii host image write`

- Mutating: `yes`
- Interactive terminal: `yes`

```text
usage: iii host image write [-h] --image IMAGE --bootstrap-input
                            BOOTSTRAP_INPUT [--schema-root SCHEMA_ROOT]
                            --device DEVICE --evidence-directory
                            EVIDENCE_DIRECTORY
                            (--backup-record BACKUP_RECORD | --accept-data-loss)

options:
  -h, --help            show this help message and exit
  --image IMAGE         downloaded pinned .img.xz file
  --bootstrap-input BOOTSTRAP_INPUT
                        Git-ignored owner-only per-imaging JSON input
  --schema-root SCHEMA_ROOT
                        override deployment schema directory
  --device DEVICE       exact enumerated /dev/disk/by-id path
  --evidence-directory EVIDENCE_DIRECTORY
                        owner-controlled directory for the immutable imaging
                        record
  --backup-record BACKUP_RECORD
                        verified pre-reimage host backup/salvage record
  --accept-data-loss    separately acknowledge unrecoverable source media in
                        the typed proof
```

## `iii host inspect`

- Mutating: `no`
- Interactive terminal: `no`

```text
usage: iii host inspect [-h] [--target {real,opti_track,hil}]
                        [--capture CAPTURE]

options:
  -h, --help            show this help message and exit
  --target {real,opti_track,hil}
  --capture CAPTURE     create one owner-only canonical capture; existing
                        files are never overwritten
```

## `iii host maintenance apply`

- Mutating: `yes`
- Interactive terminal: `no`

```text
usage: iii host maintenance apply [-h] --kind
                                  {packages,boot-settings,bundle-trust,release-status-trust}
                                  [--target {real,opti_track,hil}] [--offline]
                                  [--backup-record BACKUP_RECORD]
                                  [--boot-profile BOOT_PROFILE]
                                  [--trust-store TRUST_STORE]
                                  [--release-status-index RELEASE_STATUS_INDEX]
                                  [--retire-signer RETIRE_SIGNER]
                                  [--replacement-proof REPLACEMENT_PROOF]
                                  [--policy POLICY]

options:
  -h, --help            show this help message and exit
  --kind {packages,boot-settings,bundle-trust,release-status-trust}
  --target {real,opti_track,hil}
  --offline
  --backup-record BACKUP_RECORD
  --boot-profile BOOT_PROFILE
                        exact content-identified Raspberry Pi boot profile
  --trust-store TRUST_STORE
  --release-status-index RELEASE_STATUS_INDEX
                        replacement index signed by the newly active release-
                        status signer
  --retire-signer RETIRE_SIGNER
  --replacement-proof REPLACEMENT_PROOF
                        canonical public proof-of-possession for one new
                        active signer
  --policy POLICY       explicit reviewed host-maintenance policy
```

## `iii host maintenance check`

- Mutating: `no`
- Interactive terminal: `no`

```text
usage: iii host maintenance check [-h] --kind
                                  {packages,boot-settings,bundle-trust,release-status-trust}
                                  [--target {real,opti_track,hil}] [--offline]
                                  [--backup-record BACKUP_RECORD]
                                  [--boot-profile BOOT_PROFILE]
                                  [--trust-store TRUST_STORE]
                                  [--release-status-index RELEASE_STATUS_INDEX]
                                  [--retire-signer RETIRE_SIGNER]
                                  [--replacement-proof REPLACEMENT_PROOF]
                                  [--policy POLICY]

options:
  -h, --help            show this help message and exit
  --kind {packages,boot-settings,bundle-trust,release-status-trust}
  --target {real,opti_track,hil}
  --offline
  --backup-record BACKUP_RECORD
  --boot-profile BOOT_PROFILE
                        exact content-identified Raspberry Pi boot profile
  --trust-store TRUST_STORE
  --release-status-index RELEASE_STATUS_INDEX
                        replacement index signed by the newly active release-
                        status signer
  --retire-signer RETIRE_SIGNER
  --replacement-proof REPLACEMENT_PROOF
                        canonical public proof-of-possession for one new
                        active signer
  --policy POLICY       explicit reviewed host-maintenance policy
```

## `iii host maintenance reboot`

- Mutating: `yes`
- Interactive terminal: `no`

```text
usage: iii host maintenance reboot [-h] --maintenance-id MAINTENANCE_ID
                                   [--target {real,opti_track,hil}]

options:
  -h, --help            show this help message and exit
  --maintenance-id MAINTENANCE_ID
  --target {real,opti_track,hil}
```

## `iii host maintenance status`

- Mutating: `no`
- Interactive terminal: `no`

```text
usage: iii host maintenance status [-h] [--target {real,opti_track,hil}]

options:
  -h, --help            show this help message and exit
  --target {real,opti_track,hil}
```

## `iii host network apply`

- Mutating: `yes`
- Interactive terminal: `no`

```text
usage: iii host network apply [-h] --input INPUT
                              [--target {real,opti_track,hil}]

options:
  -h, --help            show this help message and exit
  --input INPUT         Git-ignored owner-only network JSON
  --target {real,opti_track,hil}
```

## `iii host network confirm`

- Mutating: `yes`
- Interactive terminal: `no`

```text
usage: iii host network confirm [-h] --network-operation-id
                                NETWORK_OPERATION_ID
                                [--target {real,opti_track,hil}]

options:
  -h, --help            show this help message and exit
  --network-operation-id NETWORK_OPERATION_ID
  --target {real,opti_track,hil}
```

## `iii host network status`

- Mutating: `no`
- Interactive terminal: `no`

```text
usage: iii host network status [-h] --network-operation-id
                               NETWORK_OPERATION_ID
                               [--target {real,opti_track,hil}]

options:
  -h, --help            show this help message and exit
  --network-operation-id NETWORK_OPERATION_ID
  --target {real,opti_track,hil}
```

## `iii host provision apply`

- Mutating: `yes`
- Interactive terminal: `no`

```text
usage: iii host provision apply [-h] --target TARGET --inventory INVENTORY
                                --inputs INPUTS [--schema-root SCHEMA_ROOT]
                                [--ansible-playbook ANSIBLE_PLAYBOOK]

options:
  -h, --help            show this help message and exit
  --target TARGET       exact Ansible inventory host or pattern
  --inventory INVENTORY
                        audited Ansible inventory
  --inputs INPUTS       owner-only Git-ignored host-provisioning JSON input
  --schema-root SCHEMA_ROOT
                        override deployment schema directory
  --ansible-playbook ANSIBLE_PLAYBOOK
                        explicit audited ansible-playbook executable
```

## `iii host provision check`

- Mutating: `no`
- Interactive terminal: `no`

```text
usage: iii host provision check [-h] --target TARGET --inventory INVENTORY
                                --inputs INPUTS [--schema-root SCHEMA_ROOT]
                                [--ansible-playbook ANSIBLE_PLAYBOOK]

options:
  -h, --help            show this help message and exit
  --target TARGET       exact Ansible inventory host or pattern
  --inventory INVENTORY
                        audited Ansible inventory
  --inputs INPUTS       owner-only Git-ignored host-provisioning JSON input
  --schema-root SCHEMA_ROOT
                        override deployment schema directory
  --ansible-playbook ANSIBLE_PLAYBOOK
                        explicit audited ansible-playbook executable
```

## `iii host salvage`

- Mutating: `yes`
- Interactive terminal: `yes`

```text
usage: iii host salvage [-h] [--registry-root REGISTRY_ROOT] --device DEVICE

options:
  -h, --help            show this help message and exit
  --registry-root REGISTRY_ROOT
  --device DEVICE
```

## `iii logs prune`

- Mutating: `yes`
- Interactive terminal: `no`

```text
usage: iii logs prune [-h] --pulled RECEIPT_ID [--target {sim,real,hil}]

options:
  -h, --help            show this help message and exit
  --pulled RECEIPT_ID
  --target {sim,real,hil}
```

## `iii logs pull`

- Mutating: `yes`
- Interactive terminal: `no`

```text
usage: iii logs pull [-h] [--destination DESTINATION]
                     [--target {sim,real,hil}]

options:
  -h, --help            show this help message and exit
  --destination DESTINATION
  --target {sim,real,hil}
```

## `iii mission list`

- Mutating: `no`
- Interactive terminal: `no`

```text
usage: iii mission list [-h] [--all]

options:
  -h, --help  show this help message and exit
  --all       Include incompatible classified local entries with reasons
```

## `iii mission select`

- Mutating: `yes`
- Interactive terminal: `no`

```text
usage: iii mission select [-h] [--default] [catalog_id]

positional arguments:
  catalog_id

options:
  -h, --help  show this help message and exit
  --default   Restore the profile default
```

## `iii mission show`

- Mutating: `no`
- Interactive terminal: `no`

```text
usage: iii mission show [-h] catalog_id

positional arguments:
  catalog_id

options:
  -h, --help  show this help message and exit
```

## `iii mission status`

- Mutating: `no`
- Interactive terminal: `no`

```text
usage: iii mission status [-h]

options:
  -h, --help  show this help message and exit
```

## `iii px4 params apply`

- Mutating: `yes`
- Interactive terminal: `no`

```text
usage: iii px4 params apply [-h] --plan-id PLAN_ID --key KEY

options:
  -h, --help         show this help message and exit
  --plan-id PLAN_ID
  --key KEY
```

## `iii px4 params capture`

- Mutating: `yes`
- Interactive terminal: `no`

```text
usage: iii px4 params capture [-h] --snapshot SNAPSHOT --name NAME
                              --description DESCRIPTION

options:
  -h, --help            show this help message and exit
  --snapshot SNAPSHOT
  --name NAME
  --description DESCRIPTION
```

## `iii px4 params diff`

- Mutating: `no`
- Interactive terminal: `no`

```text
usage: iii px4 params diff [-h] --left LEFT --right RIGHT

options:
  -h, --help     show this help message and exit
  --left LEFT
  --right RIGHT
```

## `iii px4 params export`

- Mutating: `yes`
- Interactive terminal: `no`

```text
usage: iii px4 params export [-h] --capture-id CAPTURE_ID --destination
                             DESTINATION

options:
  -h, --help            show this help message and exit
  --capture-id CAPTURE_ID
  --destination DESTINATION
```

## `iii px4 params import`

- Mutating: `yes`
- Interactive terminal: `no`

```text
usage: iii px4 params import [-h] --source SOURCE

options:
  -h, --help       show this help message and exit
  --source SOURCE
```

## `iii px4 params list`

- Mutating: `no`
- Interactive terminal: `no`

```text
usage: iii px4 params list [-h]

options:
  -h, --help  show this help message and exit
```

## `iii px4 params plan`

- Mutating: `no`
- Interactive terminal: `no`

```text
usage: iii px4 params plan [-h] --profile {real,sim} --snapshot SNAPSHOT
                           [--key KEY]

options:
  -h, --help            show this help message and exit
  --profile {real,sim}
  --snapshot SNAPSHOT
  --key KEY
```

## `iii px4 params promote`

- Mutating: `yes`
- Interactive terminal: `no`

```text
usage: iii px4 params promote [-h] --capture-id CAPTURE_ID
                              (--key KEY | --all-defaults)

options:
  -h, --help            show this help message and exit
  --capture-id CAPTURE_ID
  --key KEY
  --all-defaults        promote every non-calibration manifest value from the
                        complete capture
```

## `iii px4 params pull`

- Mutating: `no`
- Interactive terminal: `no`

```text
usage: iii px4 params pull [-h] --profile {real,sim} [--release-id RELEASE_ID]

options:
  -h, --help            show this help message and exit
  --profile {real,sim}
  --release-id RELEASE_ID
                        exact staged release used for receiver-owned real or
                        HIL PX4 Ethernet capture
```

## `iii px4 params show`

- Mutating: `no`
- Interactive terminal: `no`

```text
usage: iii px4 params show [-h] --capture-id CAPTURE_ID

options:
  -h, --help            show this help message and exit
  --capture-id CAPTURE_ID
```

## `iii px4 params verify`

- Mutating: `no`
- Interactive terminal: `no`

```text
usage: iii px4 params verify [-h] --plan-id PLAN_ID

options:
  -h, --help         show this help message and exit
  --plan-id PLAN_ID
```

## `iii px4 release audit`

- Mutating: `no`
- Interactive terminal: `no`

```text
usage: iii px4 release audit [-h] --release-id RELEASE_ID

options:
  -h, --help            show this help message and exit
  --release-id RELEASE_ID
```

## `iii px4 release prepare`

- Mutating: `yes`
- Interactive terminal: `no`

```text
usage: iii px4 release prepare [-h] --release-directory RELEASE_DIRECTORY
                               --destination DESTINATION

options:
  -h, --help            show this help message and exit
  --release-directory RELEASE_DIRECTORY
  --destination DESTINATION
```

## `iii qgc config apply`

- Mutating: `yes`
- Interactive terminal: `no`

```text
usage: iii qgc config apply [-h] --release-id RELEASE_ID --qgc-version
                            QGC_VERSION --profile {real,sim}

options:
  -h, --help            show this help message and exit
  --release-id RELEASE_ID
  --qgc-version QGC_VERSION
  --profile {real,sim}
```

## `iii qgc config cache`

- Mutating: `yes`
- Interactive terminal: `no`

```text
usage: iii qgc config cache [-h] --source SOURCE --qgc-version QGC_VERSION
                            --px4-firmware PX4_FIRMWARE --manifest-id
                            MANIFEST_ID

options:
  -h, --help            show this help message and exit
  --source SOURCE
  --qgc-version QGC_VERSION
  --px4-firmware PX4_FIRMWARE
  --manifest-id MANIFEST_ID
```

## `iii qgc config capture`

- Mutating: `yes`
- Interactive terminal: `no`

```text
usage: iii qgc config capture [-h] --release-id RELEASE_ID --qgc-version
                              QGC_VERSION [--clean-exit]

options:
  -h, --help            show this help message and exit
  --release-id RELEASE_ID
  --qgc-version QGC_VERSION
  --clean-exit
```

## `iii qgc config diff`

- Mutating: `no`
- Interactive terminal: `no`

```text
usage: iii qgc config diff [-h] --capture-id CAPTURE_ID

options:
  -h, --help            show this help message and exit
  --capture-id CAPTURE_ID
```

## `iii qgc config promote`

- Mutating: `yes`
- Interactive terminal: `no`

```text
usage: iii qgc config promote [-h] --capture-id CAPTURE_ID --key KEY

options:
  -h, --help            show this help message and exit
  --capture-id CAPTURE_ID
  --key KEY
```

## `iii qgc config verify-cache`

- Mutating: `no`
- Interactive terminal: `no`

```text
usage: iii qgc config verify-cache [-h] --cache-id CACHE_ID --qgc-version
                                   QGC_VERSION --px4-firmware PX4_FIRMWARE
                                   --manifest-id MANIFEST_ID

options:
  -h, --help            show this help message and exit
  --cache-id CACHE_ID
  --qgc-version QGC_VERSION
  --px4-firmware PX4_FIRMWARE
  --manifest-id MANIFEST_ID
```

## `iii qgc restart`

- Mutating: `yes`
- Interactive terminal: `no`

```text
usage: iii qgc restart [-h]

options:
  -h, --help  show this help message and exit
```

## `iii qgc start`

- Mutating: `yes`
- Interactive terminal: `no`

```text
usage: iii qgc start [-h]

options:
  -h, --help  show this help message and exit
```

## `iii qgc status`

- Mutating: `no`
- Interactive terminal: `no`

```text
usage: iii qgc status [-h]

options:
  -h, --help  show this help message and exit
```

## `iii qgc stop`

- Mutating: `yes`
- Interactive terminal: `no`

```text
usage: iii qgc stop [-h]

options:
  -h, --help  show this help message and exit
```

## `iii records archive`

- Mutating: `yes`
- Interactive terminal: `no`

```text
usage: iii records archive [-h] [--registry-root REGISTRY_ROOT]
                           [--domain {archive-receipts,backups,captures,commissioning,diagnostic-pulls,gc-release-cache,import-receipts,log-pulls,operations,readiness,release-cache,release-evidence,status-indexes}]
                           [--base BASE]
                           destination

positional arguments:
  destination

options:
  -h, --help            show this help message and exit
  --registry-root REGISTRY_ROOT
                        override the user-owned registry root (Git worktrees
                        require .iii/)
  --domain {archive-receipts,backups,captures,commissioning,diagnostic-pulls,gc-release-cache,import-receipts,log-pulls,operations,readiness,release-cache,release-evidence,status-indexes}
  --base BASE           verified base archive for incremental output
```

## `iii records import`

- Mutating: `yes`
- Interactive terminal: `no`

```text
usage: iii records import [-h] [--registry-root REGISTRY_ROOT] archive

positional arguments:
  archive

options:
  -h, --help            show this help message and exit
  --registry-root REGISTRY_ROOT
                        override the user-owned registry root (Git worktrees
                        require .iii/)
```

## `iii records inventory`

- Mutating: `no`
- Interactive terminal: `no`

```text
usage: iii records inventory [-h] [--registry-root REGISTRY_ROOT]
                             [--domain {archive-receipts,backups,captures,commissioning,diagnostic-pulls,gc-release-cache,import-receipts,log-pulls,operations,readiness,release-cache,release-evidence,status-indexes}]
                             [--warning-days WARNING_DAYS]

options:
  -h, --help            show this help message and exit
  --registry-root REGISTRY_ROOT
                        override the user-owned registry root (Git worktrees
                        require .iii/)
  --domain {archive-receipts,backups,captures,commissioning,diagnostic-pulls,gc-release-cache,import-receipts,log-pulls,operations,readiness,release-cache,release-evidence,status-indexes}
  --warning-days WARNING_DAYS
```

## `iii records prune`

- Mutating: `yes`
- Interactive terminal: `no`

```text
usage: iii records prune [-h] [--registry-root REGISTRY_ROOT] --record SHA256

options:
  -h, --help            show this help message and exit
  --registry-root REGISTRY_ROOT
                        override the user-owned registry root (Git worktrees
                        require .iii/)
  --record SHA256
```

## `iii records verify`

- Mutating: `no`
- Interactive terminal: `no`

```text
usage: iii records verify [-h] [--registry-root REGISTRY_ROOT]
                          [--domain {archive-receipts,backups,captures,commissioning,diagnostic-pulls,gc-release-cache,import-receipts,log-pulls,operations,readiness,release-cache,release-evidence,status-indexes}]
                          [--warning-days WARNING_DAYS]

options:
  -h, --help            show this help message and exit
  --registry-root REGISTRY_ROOT
                        override the user-owned registry root (Git worktrees
                        require .iii/)
  --domain {archive-receipts,backups,captures,commissioning,diagnostic-pulls,gc-release-cache,import-receipts,log-pulls,operations,readiness,release-cache,release-evidence,status-indexes}
  --warning-days WARNING_DAYS
```

## `iii release cache`

- Mutating: `no`
- Interactive terminal: `no`

```text
usage: iii release cache [-h] [--repository REPOSITORY]
                         [--schema-root SCHEMA_ROOT] [--policy POLICY]
                         [--trusted-signers TRUSTED_SIGNERS]
                         [--status-trusted-signers STATUS_TRUSTED_SIGNERS]
                         [--cache-root CACHE_ROOT]
                         version

positional arguments:
  version

options:
  -h, --help            show this help message and exit
  --repository REPOSITORY
                        GitHub owner/repository
  --schema-root SCHEMA_ROOT
                        deployment schema directory
  --policy POLICY       operational policy JSON
  --trusted-signers TRUSTED_SIGNERS
                        qualified bundle signer trust store
  --status-trusted-signers STATUS_TRUSTED_SIGNERS
                        release-status signer trust store
  --cache-root CACHE_ROOT
                        qualified release cache root
```

## `iii release deploy`

- Mutating: `yes`
- Interactive terminal: `no`

```text
usage: iii release deploy [-h] --destination DESTINATION [--offline]
                          [--repository REPOSITORY]
                          [--schema-root SCHEMA_ROOT] [--policy POLICY]
                          [--trusted-signers TRUSTED_SIGNERS]
                          [--status-trusted-signers STATUS_TRUSTED_SIGNERS]
                          [--cache-root CACHE_ROOT]
                          version

positional arguments:
  version

options:
  -h, --help            show this help message and exit
  --destination DESTINATION
  --offline             explicitly accept the last verified cached status
  --repository REPOSITORY
                        GitHub owner/repository
  --schema-root SCHEMA_ROOT
                        deployment schema directory
  --policy POLICY       operational policy JSON
  --trusted-signers TRUSTED_SIGNERS
                        qualified bundle signer trust store
  --status-trusted-signers STATUS_TRUSTED_SIGNERS
                        release-status signer trust store
  --cache-root CACHE_ROOT
                        qualified release cache root
```

## `iii release fetch`

- Mutating: `yes`
- Interactive terminal: `no`

```text
usage: iii release fetch [-h] [--repository REPOSITORY]
                         [--schema-root SCHEMA_ROOT] [--policy POLICY]
                         [--trusted-signers TRUSTED_SIGNERS]
                         [--status-trusted-signers STATUS_TRUSTED_SIGNERS]
                         [--cache-root CACHE_ROOT]
                         version

positional arguments:
  version

options:
  -h, --help            show this help message and exit
  --repository REPOSITORY
                        GitHub owner/repository
  --schema-root SCHEMA_ROOT
                        deployment schema directory
  --policy POLICY       operational policy JSON
  --trusted-signers TRUSTED_SIGNERS
                        qualified bundle signer trust store
  --status-trusted-signers STATUS_TRUSTED_SIGNERS
                        release-status signer trust store
  --cache-root CACHE_ROOT
                        qualified release cache root
```

## `iii release list`

- Mutating: `no`
- Interactive terminal: `no`

```text
usage: iii release list [-h] [--repository REPOSITORY]
                        [--schema-root SCHEMA_ROOT] [--policy POLICY]
                        [--trusted-signers TRUSTED_SIGNERS]
                        [--status-trusted-signers STATUS_TRUSTED_SIGNERS]
                        [--cache-root CACHE_ROOT]

options:
  -h, --help            show this help message and exit
  --repository REPOSITORY
                        GitHub owner/repository
  --schema-root SCHEMA_ROOT
                        deployment schema directory
  --policy POLICY       operational policy JSON
  --trusted-signers TRUSTED_SIGNERS
                        qualified bundle signer trust store
  --status-trusted-signers STATUS_TRUSTED_SIGNERS
                        release-status signer trust store
  --cache-root CACHE_ROOT
                        qualified release cache root
```

## `iii release show`

- Mutating: `no`
- Interactive terminal: `no`

```text
usage: iii release show [-h] [--offline] [--repository REPOSITORY]
                        [--schema-root SCHEMA_ROOT] [--policy POLICY]
                        [--trusted-signers TRUSTED_SIGNERS]
                        [--status-trusted-signers STATUS_TRUSTED_SIGNERS]
                        [--cache-root CACHE_ROOT]
                        version

positional arguments:
  version

options:
  -h, --help            show this help message and exit
  --offline             use only the verified local cache
  --repository REPOSITORY
                        GitHub owner/repository
  --schema-root SCHEMA_ROOT
                        deployment schema directory
  --policy POLICY       operational policy JSON
  --trusted-signers TRUSTED_SIGNERS
                        qualified bundle signer trust store
  --status-trusted-signers STATUS_TRUSTED_SIGNERS
                        release-status signer trust store
  --cache-root CACHE_ROOT
                        qualified release cache root
```

## `iii release status set`

- Mutating: `yes`
- Interactive terminal: `no`

```text
usage: iii release status set [-h] --status {withdrawn,unsafe} --reason REASON
                              [--superseding-version SUPERSEDING_VERSION]
                              [--repository REPOSITORY]
                              [--schema-root SCHEMA_ROOT] [--policy POLICY]
                              [--trusted-signers TRUSTED_SIGNERS]
                              [--status-trusted-signers STATUS_TRUSTED_SIGNERS]
                              [--cache-root CACHE_ROOT]
                              version

positional arguments:
  version

options:
  -h, --help            show this help message and exit
  --status {withdrawn,unsafe}
  --reason REASON
  --superseding-version SUPERSEDING_VERSION
  --repository REPOSITORY
                        GitHub owner/repository
  --schema-root SCHEMA_ROOT
                        deployment schema directory
  --policy POLICY       operational policy JSON
  --trusted-signers TRUSTED_SIGNERS
                        qualified bundle signer trust store
  --status-trusted-signers STATUS_TRUSTED_SIGNERS
                        release-status signer trust store
  --cache-root CACHE_ROOT
                        qualified release cache root
```

## `iii release verify`

- Mutating: `no`
- Interactive terminal: `no`

```text
usage: iii release verify [-h] [--offline] [--repository REPOSITORY]
                          [--schema-root SCHEMA_ROOT] [--policy POLICY]
                          [--trusted-signers TRUSTED_SIGNERS]
                          [--status-trusted-signers STATUS_TRUSTED_SIGNERS]
                          [--cache-root CACHE_ROOT]
                          version

positional arguments:
  version

options:
  -h, --help            show this help message and exit
  --offline             verify only the complete local cache
  --repository REPOSITORY
                        GitHub owner/repository
  --schema-root SCHEMA_ROOT
                        deployment schema directory
  --policy POLICY       operational policy JSON
  --trusted-signers TRUSTED_SIGNERS
                        qualified bundle signer trust store
  --status-trusted-signers STATUS_TRUSTED_SIGNERS
                        release-status signer trust store
  --cache-root CACHE_ROOT
                        qualified release cache root
```

## `iii system attach`

- Mutating: `no`
- Interactive terminal: `yes`

```text
usage: iii system attach [-h]

options:
  -h, --help  show this help message and exit
```

## `iii system boot`

- Mutating: `yes`
- Interactive terminal: `no`

```text
usage: iii system boot [-h] [--attach] [--profile {sim,real,opti_track,hil}]

options:
  -h, --help            show this help message and exit
  --attach              Attach to the tmux session after booting.
  --profile {sim,real,opti_track,hil}
                        explicit cold runtime profile for this boot only
```

## `iii system clock sync`

- Mutating: `yes`
- Interactive terminal: `no`

```text
usage: iii system clock sync [-h] [--target {sim,real}]
                             [--profile {real,opti_track}]

options:
  -h, --help            show this help message and exit
  --target {sim,real}
  --profile {real,opti_track}
```

## `iii system daemon logs`

- Mutating: `no`
- Interactive terminal: `no`

```text
usage: iii system daemon logs [-h] [--follow]

options:
  -h, --help  show this help message and exit
  --follow    Follow daemon journal logs.
```

## `iii system daemon restart`

- Mutating: `yes`
- Interactive terminal: `no`

```text
usage: iii system daemon restart [-h]

options:
  -h, --help  show this help message and exit
```

## `iii system daemon start`

- Mutating: `yes`
- Interactive terminal: `no`

```text
usage: iii system daemon start [-h]

options:
  -h, --help  show this help message and exit
```

## `iii system daemon status`

- Mutating: `no`
- Interactive terminal: `no`

```text
usage: iii system daemon status [-h]

options:
  -h, --help  show this help message and exit
```

## `iii system daemon stop`

- Mutating: `yes`
- Interactive terminal: `no`

```text
usage: iii system daemon stop [-h]

options:
  -h, --help  show this help message and exit
```

## `iii system kill-session`

- Mutating: `yes`
- Interactive terminal: `no`

```text
usage: iii system kill-session [-h]

options:
  -h, --help  show this help message and exit
```

## `iii system list-nodes`

- Mutating: `no`
- Interactive terminal: `no`

```text
usage: iii system list-nodes [-h]

options:
  -h, --help  show this help message and exit
```

## `iii system list-services`

- Mutating: `no`
- Interactive terminal: `no`

```text
usage: iii system list-services [-h]

options:
  -h, --help  show this help message and exit
```

## `iii system logs`

- Mutating: `no`
- Interactive terminal: `no`

```text
usage: iii system logs [-h] [--follow] [--history] [--lines LINES] entity_id

positional arguments:
  entity_id      Entity identifier from the system specification.

options:
  -h, --help     show this help message and exit
  --follow       Follow the latest log file.
  --history      Show the accumulated process history instead of the current
                 process run.
  --lines LINES  Number of trailing log lines to print when not following.
```

## `iii system restart`

- Mutating: `yes`
- Interactive terminal: `no`

```text
usage: iii system restart [-h] [--cold]
                          [--select-nodes SELECT_NODES [SELECT_NODES ...]]
                          [--include-dependencies]

options:
  -h, --help            show this help message and exit
  --cold                Cleanup before starting again.
  --select-nodes SELECT_NODES [SELECT_NODES ...]
                        Restart the specified nodes.
  --include-dependencies
                        Will also restart selected nodes dependencies.
```

## `iii system service list`

- Mutating: `no`
- Interactive terminal: `no`

```text
usage: iii system service list [-h]

options:
  -h, --help  show this help message and exit
```

## `iii system service restart`

- Mutating: `yes`
- Interactive terminal: `no`

```text
usage: iii system service restart [-h] service_id

positional arguments:
  service_id  Service identifier from the system specification.

options:
  -h, --help  show this help message and exit
```

## `iii system service start`

- Mutating: `yes`
- Interactive terminal: `no`

```text
usage: iii system service start [-h] service_id

positional arguments:
  service_id  Service identifier from the system specification.

options:
  -h, --help  show this help message and exit
```

## `iii system service stop`

- Mutating: `yes`
- Interactive terminal: `no`

```text
usage: iii system service stop [-h] service_id

positional arguments:
  service_id  Service identifier from the system specification.

options:
  -h, --help  show this help message and exit
```

## `iii system shutdown`

- Mutating: `yes`
- Interactive terminal: `no`

```text
usage: iii system shutdown [-h] [--keep-session]
                           [--select-nodes SELECT_NODES [SELECT_NODES ...]]
                           [--include-dependencies]

options:
  -h, --help            show this help message and exit
  --keep-session        Keep the tmux session after shutting down the full
                        system runtime.
  --select-nodes SELECT_NODES [SELECT_NODES ...]
                        Shutdown the specified nodes.
  --include-dependencies
                        Will also shutdown selected nodes dependencies.
```

## `iii system start`

- Mutating: `yes`
- Interactive terminal: `no`

```text
usage: iii system start [-h] [--skip-activate]
                        [--select-nodes SELECT_NODES [SELECT_NODES ...]]
                        [--include-dependencies]

options:
  -h, --help            show this help message and exit
  --skip-activate       Will only configure the system without activating it.
  --select-nodes SELECT_NODES [SELECT_NODES ...]
                        Start the specified nodes.
  --include-dependencies
                        Will also start selected nodes dependencies.
```

## `iii system status`

- Mutating: `no`
- Interactive terminal: `no`

```text
usage: iii system status [-h] [--watch]

options:
  -h, --help  show this help message and exit
  --watch     Refresh status continuously.
```

## `iii system stop`

- Mutating: `yes`
- Interactive terminal: `no`

```text
usage: iii system stop [-h] [--skip-cleanup]
                       [--select-nodes SELECT_NODES [SELECT_NODES ...]]
                       [--include-dependencies]

options:
  -h, --help            show this help message and exit
  --skip-cleanup        Will only deactivate the system without cleaning it
                        up.
  --select-nodes SELECT_NODES [SELECT_NODES ...]
                        Stop the specified nodes.
  --include-dependencies
                        Will also stop selected nodes dependencies.
```

## `iii verify deployment`

- Mutating: `no`
- Interactive terminal: `no`

```text
usage: iii verify deployment [-h] [--root ROOT] [--evidence EVIDENCE]
                             [--trusted-signers TRUSTED_SIGNERS]
                             [--junit JUNIT] [--report REPORT]
                             [--require-level {host-independent,target-equivalent,physical}]
                             [--require-complete] [--audit-only]

options:
  -h, --help            show this help message and exit
  --root ROOT
  --evidence EVIDENCE
  --trusted-signers TRUSTED_SIGNERS
  --junit JUNIT
  --report REPORT
  --require-level {host-independent,target-equivalent,physical}
  --require-complete
  --audit-only          validate reviewed definitions without claiming
                        execution evidence
```
